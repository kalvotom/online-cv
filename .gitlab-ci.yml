###
#
# Build site.
#

build:
  image: ruby:slim
  script:
    - apt-get install build-essentials
    - gem install jekyll
    - jekyll build -d public
  artifacts:
    paths:
      - public
    expire_in: 1 day

